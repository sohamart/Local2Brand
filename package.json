{
  "name": "local2brand-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "if [ -d \"frontend\" ]; then cd frontend && npm install && npm run build && cd .. && mkdir -p dist && cp -r frontend/dist/* dist/ 2>/dev/null || true; else npm install && npm run build; fi",
    "install:all": "cd frontend && npm install && cd ../backend && npm install"
  }
}
